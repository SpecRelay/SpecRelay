# User Request — SpecRelay Task 9003-dogfood-scenario-c

Spec source: docs/sdd/9003-dogfood-scenario-c/spec.md

# Dogfood Scenario C — interruption then safe resume

Append a line to INTERRUPT-FIXTURE.txt at the repository root. This is a
deliberately trivial, bounded, safe fixture spec used only to dogfood
SpecRelay's interruption/resume safety (SDD 0085 dogfood scenario C) inside
an isolated harness — it is not real product work.
