
# SpecRelay local task run evidence
.specrelay-runs/

# SpecRelay local test cache / timing profile (spec 0016). Reusable local
# profiling data only (latest.json test timings); never committed.
.specrelay-cache/

# Python bytecode
__pycache__/
*.py[cod]

.mcp_data/

# Local developer configuration overlay (spec 0027) — personal-only
# overrides for THIS repository's own .specrelay/config.yml; never commit.
.specrelay/config.local.yml
